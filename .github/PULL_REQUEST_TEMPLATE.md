## What changed

<!-- One or two sentences. -->

## Checklist

- [ ] `make check` passes locally (lint, all `ci/` scenarios, negative values, name limits)
- [ ] `make check-lock` passes if `UPSTREAM_REF` changed
- [ ] `charts/mirofish-offline/README.md` updated if values changed
- [ ] `Chart.yaml` `version` bumped if anything under `charts/` changed
